# 작업 로그 — 식음업장 메뉴 수요 예측

## 2026-06-01 — 레포 재정비 (재시작)

기존 untracked 폴더를 다른 대회 폴더 컨벤션(`data/ src/ notebooks/ docs/ submissions/`)에 맞춰 정리.

- `trial.py` → `src/baseline_lstm.py` (데이터 경로를 옛 `./train/` → `data/` 구조로 정정, pathlib 기준 경로화)
- `trial_final.ipynb` → `notebooks/`
- `baseline_submission.csv` → `submissions/`
- 신규: `README.md`, `requirements.txt`, `src/metric.py`(가중 SMAPE + `make_holdout` 로컬 검증)
- 루트 `README.md` / `.gitignore` 에 본 프로젝트 추가

### 데이터 파악 (EDA)

- train: 2023-01-01~2024-06-15 (532일), 193 메뉴 × 9 영업장, long, 모든 메뉴 풀히스토리.
- 매출수량: 평균 10.65 / 중앙값 0 / 최대 1372, **0 비율 53%**, 음수 14건(노이즈로 추정).
- 영업장 행수: 담하 22,344 > 미라시아 16,492 > 라그로타 13,300 > 카페테리아 12,768 ... 화담숲카페 2,660.
- test: TEST_00~09 각 28일 입력 → 7일 예측, 샘플 독립.

### 미해결 / 확인 필요

- **가중 SMAPE 정확 정의**: 가중 영업장(담하/미라시아?) · 가중치 값 · 0 처리 규칙을 공식 평가 코드로 확정 → `src/metric.py` 상수 갱신.
- 이전 참가 당시 점수/접근(있으면) 반영.

### 평가 규칙 확정 (공식 규정)

- 가중 SMAPE. **담하·미라시아 = 더 높은 가중치, 단 가중치 값 비공개** → 로컬 LB 절대재현 불가.
  대응: `score_per_store` 영업장별 분해를 1차 지표로, 담하·미라시아 집중.
- **실제값 0 항목은 평가 제외** (확정).
- 베이스라인(메뉴별 LSTM) 목표: **Public 0.711046 / Private 0.693935** (낮을수록 좋음).
- Public=test 50%, Private=100%. 누수 규칙: 28일만, recursive 허용, 샘플 독립, 외부데이터 금지(공휴일 등 도메인 지식 OK).

### 로컬 검증 결과 — 단순 베이스라인 (`src/baselines.py`, n_folds=4)

가중 SMAPE (LB 스케일 0~2, 낮을수록 좋음):

| 방법 | fold0(최근) | mean | std |
|---|---:|---:|---:|
| **weekday+mean7 (0.5블렌드)** | **0.7275** | **0.7050** | 0.032 |
| mean28 | 0.7436 | 0.7220 | 0.021 |
| weekday_mean | 0.7413 | 0.7468 | 0.048 |
| mean7 | 0.8142 | 0.7921 | 0.021 |
| last | 1.0171 | 0.9661 | 0.077 |
| zero | 2.0000 | 2.0000 | — |

- **스케일 교정 성공**: 로컬 0.70~0.73 ≈ LB 0.69~0.71 (같은 자릿수) → 검증 신뢰 가능.
- **핵심**: 무학습 요일평균+최근7일 블렌드(≈0.705)가 공식 LSTM 베이스라인(0.694~0.711)과 동급.
  ⇒ 메뉴별 LSTM 은 요일 평균만큼도 못 짜냄 → 글로벌 모델 명분 확보.
- 영업장별 SMAPE(fold0, worst→best): 연회장 0.96 · 카페테리아 0.83 · 느티나무BBQ 0.79 ·
  **담하 0.75(고가중)** · **미라시아 0.71(고가중)** · 라그로타 0.70 · 화담숲주막 0.51 · 화담숲카페 0.48 · 포레스트릿 0.39.

### 글로벌 LightGBM 결과 (`src/features.py`, `src/train_lgbm.py`)

direct multi-horizon(28일 윈도우 + 예측일 달력 → h=1..7 직접), log1p 타깃 L1, 667k 샘플, 33 피처.

- **LGBM 단독 = 0.8411** (홀드아웃 fold0) → 요일평균(0.7413)·블렌드(0.7275)보다 **나쁨**.
- 영업장별: 연회장 1.25 / 느티나무BBQ 1.15 / 라그로타 1.06 등 저볼륨·변동 시리즈에서 과/오예측 → SMAPE 폭발.
- 피처중요도: item > tgt_month > tgt_wd > tgt_dom. 달력·아이덴티티 의존, 윈도우 통계는 보조.
- 결론: **순수 GBDT는 이 intermittent+상대지표 문제에서 요일평균을 못 이김.** 레버는 (a) 블렌드 위 보정, (b) Tweedie, (c) SMAPE-aware 스케일.

### SMAPE 최적화 실험 (`src/experiments.py`, 4폴드)

- wd vs mean7 블렌드: best alpha=0.6 → 0.7030.
- **★3-blend: 0.4·요일평균 + 0.6·28일평균, ×1.04 → 0.6768** (4폴드 평균). mean7 가중 0 — **28일 평균이 더 안정적인 수준 추정**.
- SMAPE-aware 스케일: 우편향이라 c가 약간 >1(1.04)이 최적이지만 효과는 작음(0.679→0.677).

### ★ 현재 최고 = 무학습 블렌드 (`src/predict_blend.py`)

`0.4*weekday_mean + 0.6*mean28` (scale 1.0) → **홀드아웃 4폴드 0.6791**. 제출 `submissions/blend_submission.csv` 생성.
공식 LSTM 베이스라인(LB Public 0.711 / Private 0.694)을 로컬에서 명확히 추월. **LB 변환율 측정용 첫 제출 후보.**

### 다음 단계 (후보)

1. **blend_submission.csv 실제 제출 → LB 측정** (로컬 0.679 → LB 변환율 확보가 최우선).
2. **업장별 alpha/scale 튜닝**: 연회장·카페테리아(행사성) 따로, 화담숲(저변동) 따로.
3. **LGBM을 residual/보정으로**: target = y - 블렌드, 작은 보정만 학습. 또는 Tweedie 재시도.
4. 0 예측 게이팅(최근 28일 전부 0이면 0), 공휴일/이벤트 피처 강화.
