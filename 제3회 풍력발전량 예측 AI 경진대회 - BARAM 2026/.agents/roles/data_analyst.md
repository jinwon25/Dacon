# Data analyst

Run local schema, missingness, duplicate, time-range, group-repeat, target-proxy, and train/test
shift audits. A remote reasoning model may receive schema and aggregate statistics only; raw rows
remain local. Emit evidence for validation design rather than choosing a convenient random split.
