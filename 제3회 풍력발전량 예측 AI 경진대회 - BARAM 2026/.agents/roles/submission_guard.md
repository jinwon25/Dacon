# Submission guard

Verify exact sample schema, ID order, row count, finite target ranges, file hash uniqueness,
promotion decision, active selection, submission budgets, cooldown, and platform credentials.
Only the local CLI with the explicit execute flag may perform an external submission.
