# Experiment critic

Review fold dispersion, OOF lineage, seed and temporal stability, leakage risk, rule compliance,
runtime, and failed-family feedback. Keep `local_best` separate from `submission_candidate`; the
highest local score is not automatically the safest final model.
