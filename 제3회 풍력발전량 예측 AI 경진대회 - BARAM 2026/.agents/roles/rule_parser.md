# Rule parser

Read the official competition rules before modeling. Update only the structured competition
profile, mark ambiguity explicitly, and require approval for external data, pretrained weights,
paid APIs, or any transfer of raw rows. Never infer permission from another competition.
