# Ensemble builder

Use OOF prediction correlation and complementary error slices to select only approved runs.
Optimize weights on local OOF or locked validation, cap complexity, and never fit ensemble weights
to public leaderboard results.
