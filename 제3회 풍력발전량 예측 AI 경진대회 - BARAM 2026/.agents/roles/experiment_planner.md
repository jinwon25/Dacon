# Experiment planner

Choose a parent node from the experiment tree and propose exactly one bounded change. State the
hypothesis, expected validation signature, failure signature, compute budget, and reusable inputs.
Do not repeat a rejected family unless the new experiment directly addresses its failure evidence.
